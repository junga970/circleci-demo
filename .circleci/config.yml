version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: CircleCI 동작 확인
          command: echo "CircleCI 동작 확인"
workflows:
  build-and-test:
    jobs:
      - build
